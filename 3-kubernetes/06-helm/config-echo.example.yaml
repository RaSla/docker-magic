replicaCount: 1

service:
  port: 8080

ingress:
  enabled: false
  className: "nginx"
  hosts:
    - host: k8s.n.rasla.ru
      paths:
        - path: /echo(/|$)(.*)
          pathType: Prefix
#  annotations:
#    nginx.ingress.kubernetes.io/rewrite-target: /$2  # /api/$2

resources:
   requests:
     cpu: 10m
     memory: 32Mi
